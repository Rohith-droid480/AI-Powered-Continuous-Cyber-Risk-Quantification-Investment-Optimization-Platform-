---
trigger: always_on
---

Primary Loss ~ LogNormal(μ,σ)
  ← mean anchored to IBM India ₹255M;
     CV=2.0 and μ,σ are our assumptions/derivation

Secondary Loss = Primary Loss × 0.4
  ← our illustrative modeling assumption